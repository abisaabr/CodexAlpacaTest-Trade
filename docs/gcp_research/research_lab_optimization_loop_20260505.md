# Research Lab Optimization Loop - 2026-05-05

This handoff records the research-only optimization loop for the options backtester, promoter, data coverage chain, and paper-runner safety posture. It does not authorize live trading, paper-session restart, live-manifest edits, risk-policy edits, or relaxation of the `fill_coverage >= 0.90` gate.

## Active research wave

- Wave: `qqq_spy_fillfriendly_tournament_20260505T2035Z`
- GCS root: `gs://codexalpaca-control-us/research_results/qqq_spy_fillfriendly_tournament_20260505T2035Z/`
- QQQ worker: `qqq-spy-ff-qqq-20260505a`, `us-central1-a`, research-only
- SPY worker: `qqq-spy-ff-spy-20260505a`, `us-east1-b`, research-only
- Worker script: `scripts/gcp_single_ticker_365d_shard.sh`
- Profiles per symbol: selectors `nearest_contract` and `entry_liquidity_first_research_only` crossed with lag profiles `0:60`, `10:60`, and `30:120`
- Promotion posture: research-only. Final promotion-review packet must be generated by `scripts/build_research_promotion_review_packet.py`.

At the 2026-05-05 17:08 ET check, both workers were healthy and running. Four of six expected profile summaries had uploaded for each symbol. The missing profiles were the long-running final lag profiles. No broker-facing VM was running; the earlier QQQ/SPY paper VM was terminated.

## Interim read, not a promotion decision

Partial local mirror:

`reports/research_wave/qqq_spy_fillfriendly_tournament_20260505T2035Z_partial/`

Completed profile summaries at the interim read:

- QQQ: 4/6 profile summaries
- SPY: 4/6 profile summaries
- Combined candidate rows: 1008
- Fill-failure reason counts across completed profiles:
  - `fill_gate_clear`: 750
  - `selected_contract_universe_gap`: 232
  - `exit_bar_gap_or_exit_policy_mismatch`: 18
  - `entry_bar_gap_or_entry_timing_mismatch`: 6
  - `mixed_low_fill_gap`: 2

Strict interim screen used for triage only:

- `fill_coverage >= 0.90`
- `option_trade_count >= 20`
- `test_trade_count >= 20`
- `net_pnl > 0`
- `test_net_pnl > 0`
- `expectancy > 0`

The partial strict screen produced bull `single_leg_repair` leads for QQQ and SPY. It did not yet produce bear or choppy strict leads in the completed profiles. Final aggregation must wait for all six profiles and the generated promotion-review packets.

Representative interim leads:

- QQQ bull `single_leg_repair`, `entry_liquidity_first_research_only`, `e10x60`, fill `0.9897`, trades `192`, test trades `21`, net PnL `1522.71`, test PnL `4454.14`, candidate `portfolio12h__qqq__bull__call__single_leg_repair__e59a5796305393`
- QQQ bull `single_leg_repair`, `entry_liquidity_first_research_only`, `e10x60`, fill `0.9891`, trades `181`, test trades `21`, net PnL `4495.26`, test PnL `4234.78`, candidate `portfolio12h__qqq__bull__call__single_leg_repair__7f37aee10d5fda`
- SPY bull `single_leg_repair`, `entry_liquidity_first_research_only`, `e10x60`, fill `0.9903`, trades `102`, test trades `20`, net PnL `3723.58`, test PnL `1590.41`, candidate `portfolio12h__spy__bull__call__single_leg_repair__32002242e2b831`

## Operational patch

`scripts/gcp_single_ticker_365d_shard.sh` now writes `ticker_365d_progress_status.json` during long-running candidate loops. The status includes:

- active option-aware backtest process count
- completed candidate-summary profile count
- expected profile count
- progress-file counts
- latest candidate-progress JSONL record
- explicit research-only safety fields

This fixes a monitoring ambiguity where `ticker_365d_status.json` only changed at phase boundaries and could look stale while healthy long-running profiles were still using full CPU. The patch does not change backtest economics, fill semantics, promotion gates, live manifests, paper order behavior, or risk policy.

## Next loop

1. Wait for both QQQ and SPY workers to finish all six profiles and upload symbol-level portfolio reports plus promotion packets.
2. Mirror final worker artifacts locally and to the durable GCS handoff prefix.
3. Run strict aggregation across both symbols with the canonical 0.90 strategy-fill gate.
4. If only bull candidates clear, patch the strategy-generation side for bear/choppy rather than weakening gates.
5. Launch small, isolated follow-up waves by symbol and regime before broad all-ticker sweeps.
6. For all tickers with adequate 365-day 5x5 next-trading-day data, run bull, bear, and choppy waves in parallel shards only after the QQQ/SPY chain proves the final report and promotion packet are aligned.

## Edge-search focus

The next high-value strategy-search improvements are:

- liquidity-first single-leg calls and puts with explicit time-of-day and volatility-regime filters
- debit verticals with relaxed leg-distance templates but strict same-minute executable leg checks
- choppy structures that avoid wide multi-leg butterflies unless selected-contract coverage proves all legs are available
- regime-conditioned scoring by full PnL, test PnL, expectancy, drawdown, loser-cluster density, and strategy fill coverage
- preserving `candidate_variant_id`, `source_strategy_id`, `symbol`, `family`, `intended_regime`, selector, and lag profile from replay through promotion packet

No result in this handoff is eligible for activation unless a generated promotion-review packet later says `eligible_for_promotion_review`.
